public class RectApp {
    public static void main(String[] args) {
        Rectangle rect = new Rectangle(10, 20);
        rect.calculateArea();
        rect.printArea();
        rect.calculateParemeter();
        rect.printParemeter();
    }
}
